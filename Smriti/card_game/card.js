
let firstCard = prompt("enter age");
let secondCard = Math.floor(Math.random() * (11-2) + 2);
console.log(firstCard);
console.log(secondCard);
let ans = 0;

function sum() {
    ans = firstCard + secondCard;
    
}